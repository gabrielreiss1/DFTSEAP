/* ============================================================
   DFT — chave da API da Anthropic (modelo)

   COMO USAR
   1. Copie este arquivo para "chave-local.js", ao lado do index.html:
        cp chave-local.exemplo.js chave-local.js
   2. Troque o valor abaixo pela sua chave (começa com sk-ant-).
   3. Abra o DFT. A aba "Descobrir entregas" já vem com a chave carregada.

   O "chave-local.js" está no .gitignore e NÃO vai para o repositório.
   Este arquivo de exemplo vai — por isso ele nunca deve conter a chave real.

   Sem este arquivo o DFT continua funcionando: a chave é digitada na
   própria aba, com a opção de guardá-la no navegador.
   ============================================================ */

window.DFT_CHAVE_IA = 'sk-ant-api03-COLE-AQUI-A-SUA-CHAVE';
